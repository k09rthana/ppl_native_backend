var express = require(express);
const router = express.Router();
const API = require("../API/timeline");
const multer = require("multer");

router.post("/sign_up", function (req, res) {});

module.exports = router;
